import React from 'react';
import ReactDOM fomr 'react-dom';

const dTextEditor = React.createClass({
  render: function() {
    return (
      <div id='d-text-editor'>
      </div>
    );
  }
});

export default dTextEditor;
